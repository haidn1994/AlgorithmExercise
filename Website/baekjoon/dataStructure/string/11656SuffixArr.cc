#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

// 미완성

// 사실 접미사 배열을 만드려면 인클루드 해야할 것도 좀 되고 약간 복잡한 문법도 알아야 하는 것 같다. 
// 이건 C++의 문법영역도 어느정도 들어가는 것 같다.

string suffixs[1000];

int main(void)
{
	string s; ios_base::sync_with_stdio(false);
	int range, i, j;
	cin >> s;

	range = s.size
	for(i = 0; i < range; i++)
		for(j = i; j < range; j++)


}
